class UserCreationReqModels{
  String ? firstName;
  String ? lastName;
  String ? email;
  String ? password;

  UserCreationReqModels({
    required this.firstName,
    required this.lastName,
    required this.email,
    required this.password,
  });
}